---
description: Enhance a task description using the project's BRD enhancer MCP tool
---
1. Extract the user's query from the command (everything after `/enhance`).
2. Call the `mcp_brd-enhancer_enhance_task` tool with the query as the `task`.
3. Present the result clearly to the user, preferably in a code block or clean format so they can copy it.
