# Environment variables
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs and diagnostic output
*.log
logs.txt
latest_logs.txt
db_test_results.txt
diagnostic.txt

# OS
.DS_Store
Thumbs.db

# Lambda packages
lambda_*_package/
lambda_builds/
*.zip

# Agent backup snapshots (auto-generated, never commit)
.agent_backup_*/

# AgentCore
.bedrock_agentcore/

# Temporary files
*.tmp
*.bak

# AWS credentials and secrets — NEVER commit these
credentials.txt
credentials
*.pem

# Recovered / temporary data files
BRD_recovered.json
BRD_recovered.txt
_recovered_messages.json

# Cloned upstream MCP repo (independent git repo, tracked separately)
src/mcp/

# Scratch / diagnostics (probe scripts, logs, dumps — never commit)
.scratch/
.claude/scheduled_tasks.lock

# Stray Node artifacts — this is a Python repo; these landed here by mistake
node_modules/
/package.json
/package-lock.json
