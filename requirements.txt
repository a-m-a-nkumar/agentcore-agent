bedrock-agentcore==1.1.1
# Pinned to a proven-compatible set (matches the working runtime). These three
# drive most of the dependency graph (boto3/mcp/openai/pydantic via strands +
# bedrock-agentcore). Left unbounded, pip's resolver backtracks through 90+
# boto3 / 30+ strands / 16+ mcp versions and CI fails with "resolution-too-deep"
# under pip 26. Bump these deliberately (ideally via a lockfile), not by hand.
strands-agents[openai]==1.34.1
boto3>=1.40.52,<1.43
fastapi>=0.115.0
uvicorn[standard]>=0.34.2
python-docx>=1.1.0,<2.0.0
cairosvg>=2.7,<3.0  # SAD DOCX export: SVG → PNG for embedded architecture diagram
pypdf>=4.0.0
python-multipart>=0.0.6,<1.0.0
python-dotenv>=1.0.0
requests>=2.31.0,<3.0.0
PyJWT>=2.8.0,<3.0.0
cryptography>=43.0.0
jinja2>=3.0.0
psycopg2-binary>=2.9.9
langfuse>=2.0.0
mcp>=1.25,<1.28  # bounded with strands/boto3 above — see resolver note at top
httpx>=0.27.0
langchain-text-splitters>=0.3.0
tiktoken>=0.7.0
pyyaml>=6.0.0

# Terraform security scanning. NOT included here because checkov 3.2.526
# pins boto3==1.35.49, which conflicts with bedrock-agentcore>=1.1.1 (needs
# boto3>=1.40.52). Install separately in environments that need it:
#   pip install -r requirements-terraform.txt
# The terraform_generator router gracefully reports "checkov_unavailable"
# when the import fails (see services/checkov_service.py:120-128).